<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_selected="true" android:color="@color/button_text_selected" />

    <!-- Pressed state -->
    <item android:state_pressed="true" android:color="@color/button_text_selected" />

    <!-- Default state -->
    <item android:color="@color/button_text_unselected" />
</selector>

